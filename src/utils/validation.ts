export const validateName = (value: string): boolean => {
    return true;
}

export const validatePhoneNumber = (value: string): boolean => {
    return true;
}

export const validateBirthDate = (date: any): boolean => {
    return true;
}